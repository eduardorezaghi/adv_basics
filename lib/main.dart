import 'package:adv_basics/quiz.dart';
import 'package:flutter/material.dart';

void main() {
  // Use the Quiz widget as the root widget,
  // which will display the SplashScreen widget.
  // This effectively means that Quiz is the entry point of the app.
  runApp(const Quiz());
}
